type: object
title: ReceiptPreCreateInfo
properties:
  numerations:
    $ref: ./Numeration.yaml
  numerations_list:
    type: array
    description: Receipt used numerations list
    nullable: true
    items:
      type: string
  rc_centers_list:
    type: array
    description: Receipt used revenue centers list
    nullable: true
    items:
      type: string
  payment_accounts_list:
    type: array
    description: Payment accounts list
    nullable: true
    items:
      $ref: ./PaymentAccount.yaml
  categories_list:
    type: array
    nullable: true
    description: Receipt categories list
    items:
      type: string
  vat_types_list:
    type: array
    description: Vat types list
    nullable: true
    items:
      $ref: ./VatType.yaml
