title: ListF24Response
description: Results list.
allOf:
  - $ref: ../schemas/Pagination.yaml
  - type: object
    properties:
      data:
        type: array
        items:
          $ref: ../schemas/F24.yaml
  - type: object
    properties:
      aggregated_data:
        type: object
        properties:
          amount:
            type: number
            description: Total amount.
