/**
 * The Scope oauth module.
 * @module oauth/Scope
 */
export class Scope {
    {% for scope in scopes %}
    /**
     * {{ scope.comment }}
     * @member {String} {{ scope.name }}
     */
    static {{ scope.name }} = '{{ scope.str }}'
    {% endfor %}
}